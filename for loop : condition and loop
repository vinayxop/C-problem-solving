#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a, b;
    scanf("%d\n%d", &a, &b);

    // Check and print for each number in the range [a, b]
    const char *words[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    for (int i = a; i <= b; i++) {
        if (i >= 1 && i <= 9) {
            // Print the English word for the digit
            printf("%s\n", words[i - 1]);
        }
    }

    // Check and print if the second number (b) is even or odd
    for (int i = a ; i <= b; i++)
    {
        
        if (i > 9)
        {
        if (i % 2 == 0)
        printf("even\n");
    else
        printf("odd\n");
    }
    }
    return 0;
}
